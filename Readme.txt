A project to hold vimrc and bash_profile.
Copy the files into your home directory and add a . before the file names 
hide the file.

Example to copy bash_profile to homedirectory and hide.
cp bash_profile ~/.bash_profile
